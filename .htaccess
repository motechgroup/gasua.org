<IfModule mod_rewrite.c>
    RewriteEngine On

    # Prevent infinite rewrite loop if URL already targets public folder
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
